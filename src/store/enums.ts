export enum StoreSegments {
  CURRENCY_SELECTED_SOURCE = "currencySelectedSource",
  CURRENCY_SELECTED_TARGET = "currencySelectedTarget",
  CURRENCY_SELECTED_TARGETS = "currencySelectedTargets",
  AMOUNT = "amount",
}
